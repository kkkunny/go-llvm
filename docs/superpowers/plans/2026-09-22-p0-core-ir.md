# P0 类型系统骨架 + 高频 IR 构建 实现计划

> **面向 AI 代理的工作者：** 必需子技能：superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法跟踪进度。

**目标：** 落地全泛型核心（`Type[T]`/`Value[T]`）、混合式错误模型、生命周期托管，并在 `llvm/ir` 完成高频 IR 构建 + Go 类型映射，golden 测试覆盖。

**架构：** root `llvm` 定义核心词汇（Kind/Type/Value/常量/Context/Error），`llvm/ir` 定义 Module/Builder/指令角色；跨包生命周期经 `Lifetime` 令牌消除反向依赖；`internal/binding` 保持 1:1 cgo。

**技术栈：** Go 1.27（泛型方法）、cgo、LLVM 22（C API + 少量 C++ shim）、标准 `testing`（golden 对比 `Module.String()`）。

**规格：** `docs/superpowers/specs/2026-09-22-go-llvm-inkwell-style-redesign-design.md`

---

## 文件结构

```
go.mod                        go 1.27
doc.go                        包文档（用法概览，中文）
error.go                      ErrKind / Error / Catch / Must / errPanic
lifetime.go                   Lifetime 令牌
context.go                    Context（所有权根、Own/Close 级联、存活检查）
fatal.go                      fatal handler（修复旧 error.go 的逻辑反转 bug）
config.go                     版本/宿主信息（原样保留）
kind.go                       Kind 约束与 12 个标记类型
type.go                       Type[T] / AnyType / IntType 等 8 个类型角色 / 构造器
value.go                      Value[T] / AnyValue / As / lookup / 桥接 API
constant.go                   IntConst / FloatConst 等常量角色与构造器
gotype.go                     Go 类型↔LLVM 类型映射（TypeOf/ConstOf/签名映射）
ir/module.go                  Module（Verify/Print/SetSource/SetTargetTriple）
ir/function.go                Function / Func[F] / Param
ir/block.go                   Block
ir/global.go                  Global
ir/builder.go                 Builder / 定位 / 统一预检
ir/inst.go                    角色：Alloca / Load[T] / Store / Call[T] / Phi[T] / Switch
ir/builder_ctrl.go            Ret/RetVoid/Br/CondBr/Switch/Unreachable
ir/builder_arith.go           算术/位运算/ICmp/FCmp/Select
ir/builder_mem.go             Alloca/Load/Store/GEP/Mem*/Malloc/Free
ir/builder_cast.go            全部类型转换
ir/builder_call.go            Call / ExtractValue / InsertValue
ir/gofunc.go                  Module.NewFunc[F]（Go 签名→FunctionType）
*_test.go                     各包 golden / 单元测试（标准库 testing）
```

**设计约束（任务中反复用到）：**

1. **类别专属操作只放在角色包装上**（Go 不能给 `Value[IntT]` 单独加方法）。规则：`Value[T]`/`Type[T]` 本体只有通用操作（`String/Name/SetName/Type/As/Dyn/Alive/Context/Ref/IsNil`）；语义操作放角色（`IntType.Bits()`、`Alloca.SetAlign()`、`Phi.AddIncoming()`）。
2. **程序员错误 → `panic(*Error)`**（跨 Context、use-after-free、未定位 Builder、操作数语义不符）；**数据驱动的不支持/映射失败 → `error`**（`TypeOf`/`NewFunc[F]` 遇 Go 侧 string/map 等）。
3. Builder 全部方法进 cgo 前走统一预检 `b.pre(op, vs ...AnyValue)`。
4. 枚举常量一律 `= C.Name` 形式（AGENTS.md 约定），禁止手抄数值。

**开工前置（任务 1 内完成）：** 删除旧 root 包实现文件（`block.go`/`builder.go`/`constant.go`/`context.go`/`error.go`/`execution*.go,c,h`/`global.go`/`instruction.go`/`module.go`/`pass.go`/`target.go`/`type.go`/`util.go`/`value.go`），保留 `config.go`、`internal/`、`Makefile`、`README.md`、`AGENTS.md`。删除后至任务 12 之间库功能不完整属预期，但每个任务结束时必须 `go build ./...` + `go test ./...` 全绿。

---

## 核心定义（任务 1-5 共用，实现时逐字采用）

### error.go

```go
package llvm

// ErrKind 错误类别
type ErrKind int

const (
	ErrTypeMismatch ErrKind = iota // 操作数/种类不匹配
	ErrCrossContext                // 跨 Context 混用
	ErrUseAfterFree                // 使用已释放资源
	ErrClosed                      // 重复 Close
	ErrNotFound                    // 查找失败
	ErrInvalidArg                  // 参数非法
	ErrVerify                      // IR 验证失败
	ErrUnsupported                 // 映射遇到不支持的类型
	ErrInternal                    // 兜底
)

// Error Go 化的 LLVM 错误；程序员错误经 panic 抛出，可用 Catch 收敛为 error
type Error struct {
	Reason ErrKind
	Op     string // 如 "llvm.Builder.Add"
	Msg    string
}

func (e *Error) Error() string { return fmt.Sprintf("%s: %s", e.Op, e.Msg) }

// errPanic 构造并 panic（内部用）
func errPanic(reason ErrKind, op, format string, args ...any) {
	panic(&Error{Reason: reason, Op: op, Msg: fmt.Sprintf(format, args...)})
}

// Catch 执行 fn 并将其 panic 的 *Error 收敛为 error 返回；非 *Error panic 原样重抛；无 panic 返回 nil
func Catch(fn func()) (err *Error) {
	defer func() {
		if r := recover(); r != nil {
			if e, ok := r.(*Error); ok {
				err = e
			} else {
				panic(r)
			}
		}
	}()
	fn()
	return nil
}

// Must err 非 nil 时 panic(err)，否则返回 v
func Must[T any](v T, err error) T {
	if err != nil {
		panic(err)
	}
	return v
}
```

### lifetime.go

```go
package llvm

import "sync/atomic"

// Lifetime 生命周期令牌；ir.Module 持有并传给每个值，实现 use-after-free 检测
type Lifetime struct{ dead atomic.Bool }

func NewLifetime() *Lifetime { return &Lifetime{} }

// Kill 标记失效（Close 时调用）
func (l *Lifetime) Kill() { l.dead.Store(true) }

// Alive 是否存活
func (l *Lifetime) Alive() bool { return !l.dead.Load() }
```

### context.go（骨架）

```go
package llvm

type Context struct {
	ref     binding.LLVMContextRef
	life    *Lifetime
	mu      sync.Mutex
	closers []io.Closer // Own 登记的子资源，Close 时级联关闭
}

func NewContext() *Context {
	return &Context{ref: binding.LLVMContextCreate(), life: NewLifetime()}
}

// Own 登记子资源（供 llvm/* 子包使用）；Context.Close 时按逆序级联 Close
func (ctx *Context) Own(c io.Closer) {
	ctx.mu.Lock()
	defer ctx.mu.Unlock()
	ctx.closers = append(ctx.closers, c)
}

// Close 级联关闭子资源后释放 Context；二次调用返回 ErrClosed
func (ctx *Context) Close() error {
	if !ctx.life.Alive() {
		return &Error{Reason: ErrClosed, Op: "llvm.Context.Close", Msg: "context already closed"}
	}
	ctx.life.Kill()
	ctx.mu.Lock()
	closers := ctx.closers
	ctx.closers = nil
	ctx.mu.Unlock()
	for i := len(closers) - 1; i >= 0; i-- {
		_ = closers[i].Close()
	}
	binding.LLVMContextDispose(ctx.ref)
	return nil
}

func (ctx *Context) Ref() binding.LLVMContextRef  { return ctx.ref }
func (ctx *Context) Lifetime() *Lifetime          { return ctx.life }
func (ctx *Context) Alive() bool                 { return ctx.life.Alive() }
```

### kind.go

```go
package llvm

// Kind 值/类型种类标记（封闭约束，不允许用户扩展）
type Kind interface{ kind() }

type (
	VoidT   struct{}
	IntT    struct{}
	FloatT  struct{}
	PtrT    struct{}
	StructT struct{}
	ArrayT  struct{}
	VecT    struct{}
	FnT     struct{}
	LabelT  struct{}
	MetaT   struct{}
	TokenT  struct{}
	DynT    struct{} // 类型擦除
)

func (VoidT) kind() {}
func (IntT) kind()  {}
// …… 12 个一一对应
```

### type.go / value.go（关键签名）

```go
// ===== 类型 =====
type Type[T Kind] struct {
	ref binding.LLVMTypeRef
	ctx *Context
}

type AnyType interface {
	Ref() binding.LLVMTypeRef
	DynType() Type[DynT]
}

func (t Type[T]) Ref() binding.LLVMTypeRef    { return t.ref }
func (t Type[T]) DynType() Type[DynT]         { return Type[DynT]{ref: t.ref, ctx: t.ctx} }
func (t Type[T]) String() string              { return binding.LLVMPrintTypeToString(t.ref) }
func (t Type[T]) Context() *Context           { return t.ctx }
func (t Type[T]) Equal(o AnyType) bool        { return binding.LLVMTypeRef equality }
func (t Type[T]) IsSized() bool
func (t Type[T]) IsNil() bool
func (t Type[T]) As[U Kind]() (Type[U], error)   // 泛型方法：运行时 kind 校验
func (t Type[T]) MustAs[U Kind]() Type[U]

// 类型角色（类别专属操作）
type VoidType   struct{ Type[VoidT] }
type IntType    struct{ Type[IntT] }
type FloatType  struct{ Type[FloatT] }
type PtrType    struct{ Type[PtrT] }
type StructType struct{ Type[StructT] }
type ArrayType  struct{ Type[ArrayT] }
type VecType    struct{ Type[VecT] }
type FnType     struct{ Type[FnT] }

func (t IntType) Bits() uint32
func (t FloatType) Kind() FloatKind                    // FloatKind 枚举 = C.Name
func (t PtrType) Addrspace() uint32
func (t StructType) Name() string; func (t StructType) Elems() []AnyType
func (t StructType) SetBody(elems []AnyType, packed bool)
func (t StructType) IsPacked() bool; func (t StructType) IsOpaque() bool
func (t ArrayType) Elem() AnyType; func (t ArrayType) Len() uint64
func (t VecType) Elem() AnyType; func (t VecType) Len() uint32
func (t FnType) Return() AnyType; func (t FnType) Params() []AnyType; func (t FnType) IsVarArg() bool

// Context 构造器（跨 Context/种类误用 → panic(*Error)）
func (ctx *Context) Void() VoidType
func (ctx *Context) Int(bits uint32) IntType
func (ctx *Context) Bool() IntType                            // Int(1)
func (ctx *Context) Float(kind FloatKind) FloatType
func (ctx *Context) Ptr(addrspace uint32) PtrType             // 不透明指针
func (ctx *Context) Struct(elems []AnyType, packed bool) StructType
func (ctx *Context) NamedStruct(name string) StructType       // 先 opaque，后 SetBody
func (ctx *Context) Array(elem AnyType, n uint64) ArrayType
func (ctx *Context) Vec(elem AnyType, n uint32) VecType
func (ctx *Context) Fn(ret AnyType, params []AnyType, vararg bool) FnType

// ===== 值 =====
type Value[T Kind] struct {
	ref  binding.LLVMValueRef
	ctx  *Context
	life *Lifetime
}

type AnyValue interface {
	Ref() binding.LLVMValueRef
	Dyn() Value[DynT]
	Alive() bool
	Context() *Context
}

func (v Value[T]) Ref() binding.LLVMValueRef   { return v.ref }
func (v Value[T]) Dyn() Value[DynT]            { return Value[DynT]{ref: v.ref, ctx: v.ctx, life: v.life} }
func (v Value[T]) Alive() bool                 { return v.ctx.Alive() && v.life.Alive() }
func (v Value[T]) Context() *Context           { return v.ctx }
func (v Value[T]) Type() Type[T]               // 依 binding.LLVMTypeOf 查 kind，不符则内部 errPanic
func (v Value[T]) String() string              // LLVMPrintValueToString
func (v Value[T]) Name() string; func (v Value[T]) SetName(name string)
func (v Value[T]) IsNil() bool
func (v Value[T]) As[U Kind]() (Value[U], error) // 泛型方法，运行时 kind 校验
func (v Value[T]) MustAs[U Kind]() Value[U]

// 桥接 API（供 llvm/* 子包使用，外部用户通常不需要）
func NewValue[T Kind](ctx *Context, life *Lifetime, ref binding.LLVMValueRef) Value[T]
func NewType[T Kind](ctx *Context, ref binding.LLVMTypeRef) Type[T]
// ValueOf 由 binding 句柄构建擦除值（指令遍历等动态来源）
func ValueOf(ctx *Context, life *Lifetime, ref binding.LLVMValueRef) Value[DynT]
func TypeOfRef(ctx *Context, ref binding.LLVMTypeRef) Type[DynT]
```

**kind 判定统一走两张表**（`type.go` 内部函数）：`binding.LLVMTypeKind → Kind`、`binding.LLVMValueKind + opcode → 角色/值`。未知 kind 一律降级 `DynT`/`Value[DynT]`，不 panic。

### 统一预检（ir/builder.go）

```go
// pre 所有 Builder 方法入口统一调用；v 可为 nil（无操作数指令）
func (b *Builder) pre(op string, vs ...AnyValue) {
	if b.inserted == nil { // 未 MoveToEnd
		errPanic(ErrInvalidArg, op, "builder is not positioned at any block")
	}
	for _, v := range vs {
		if v == nil {
			continue
		}
		if !v.Alive() {
			errPanic(ErrUseAfterFree, op, "operand is freed")
		}
		if v.Context() != b.ctx {
			errPanic(ErrCrossContext, op, "operand belongs to another context")
		}
	}
}
// 同类预检函数：preSameType(op, l, r AnyValue)（Equal 校验）、preAlign(op, n uint32)（2 的幂）
```

---

## P0 公开 API 签名表

### 常量（constant.go，Context 方法；误用 panic）

```go
type IntConst struct{ Value[IntT] }     // SignedValue() int64 / UnsignedValue() uint64 / IsNegative() bool
type FloatConst struct{ Value[FloatT] } // Value() float64

func (ctx *Context) ConstInt(t IntType, v uint64, signed bool) IntConst      // LLVMConstInt
func (ctx *Context) ConstIntOfString(t IntType, s string, radix uint8) IntConst
func (ctx *Context) ConstBool(v bool) Value[IntT]
func (ctx *Context) ConstFloat(t FloatType, v float64) FloatConst
func (ctx *Context) ConstNull[T Kind](t Type[T]) Value[T]                    // 泛型方法
func (ctx *Context) ConstZero[T Kind](t Type[T]) Value[T]
func (ctx *Context) ConstString(s string, nullTerminate bool) Value[ArrayT]
func (ctx *Context) ConstArray(elem AnyType, elems ...AnyValue) Value[ArrayT]
func (ctx *Context) ConstStruct(packed bool, elems ...AnyValue) Value[StructT]
func (ctx *Context) ConstNamedStruct(t StructType, elems ...AnyValue) Value[StructT]
func (ctx *Context) ConstGEP[T Kind](elem AnyType, base Value[T], inBounds bool, idx ...Value[IntT]) Value[PtrT]
```

### ir 包

```go
// Module
func NewModule(ctx *llvm.Context, name string) *Module      // ctx.Own(m) 登记
func (m *Module) Close() error                              // Kill 其 Lifetime；二次 Close → ErrClosed
func (m *Module) Context() *llvm.Context
func (m *Module) Lifetime() *llvm.Lifetime
func (m *Module) String() string                            // LLVMPrintModuleToString
func (m *Module) Source() string; func (m *Module) SetSource(s string)
func (m *Module) TargetTriple() string; func (m *Module) SetTargetTriple(t string)
func (m *Module) SetDataLayout(s string)
func (m *Module) Verify() error                             // 失败 → *Error{ErrVerify, Msg=完整报告}
func (m *Module) Clone() *Module
func (m *Module) NewFunction(name string, t llvm.FnType) Function
func (m *Module) NewFunc[F any](name string) (Func[F], error)
func (m *Module) NewGlobal(name string, t llvm.AnyType) Global
func (m *Module) NewConstant(name string, v llvm.AnyValue) Global
func (m *Module) GetFunction(name string) (Function, bool)
func (m *Module) GetGlobal(name string) (Global, bool)
func (m *Module) DelGlobal(g Global)

// Function / Param / Func[F]
type Function struct{ v llvm.Value[llvm.FnT] }
func (f Function) Signature() llvm.FnType
func (f Function) Params() []Param; func (f Function) Param(i uint) Param
func (f Function) ParamAs[U llvm.Kind](i uint) llvm.Value[U]  // 泛型方法，kind 不符 panic
func (f Function) CountParams() uint
func (f Function) NewBlock(name string) Block
func (f Function) Blocks() []Block; func (f Function) EntryBlock() (Block, bool)
func (f Function) OnlyDecl() bool
func (f Function) Verify() bool
func (f Function) Linkage() Linkage; func (f Function) SetLinkage(l Linkage) // Linkage 枚举 = C.Name
func (f Function) Name() string; func (f Function) SetName(n string)
type Param struct{ v llvm.Value[llvm.DynT] }
func (p Param) SetAlign(n uint32); func (p Param) Belong() Function
type Func[F any] struct { fn Function; goType reflect.Type }
func (f Func[F]) Function() Function
func (f Func[F]) GoType() reflect.Type   // P1 JIT 桥用

// Block
type Block struct { ref binding.LLVMBasicBlockRef; ctx *llvm.Context; life *llvm.Lifetime }
func (b Block) Name() string; func (b Block) SetName(n string)
func (b Block) Belong() Function
func (b Block) Insts() []llvm.Value[llvm.DynT]  // 经 llvm.ValueOf 分发
func (b Block) FirstInst() (llvm.Value[llvm.DynT], bool); func (b Block) LastInst() (llvm.Value[llvm.DynT], bool)
func (b Block) Next() (Block, bool); func (b Block) Prev() (Block, bool)
func (b Block) Empty() bool

// Global
type Global struct{ v llvm.Value[llvm.PtrT] }
func (g Global) Initializer() (llvm.Value[llvm.DynT], bool); func (g Global) SetInitializer(v llvm.AnyValue)
func (g Global) IsConstant() bool; func (g Global) SetConstant(c bool)
func (g Global) Align() uint32; func (g Global) SetAlign(n uint32)
func (g Global) Linkage() Linkage; func (g Global) SetLinkage(l Linkage)

// Builder
func NewBuilder(ctx *llvm.Context) *Builder                       // ctx.Own(b)
func (b *Builder) Close() error
func (b *Builder) MoveToEnd(blk Block)
func (b *Builder) MoveBefore(v llvm.AnyValue)
func (b *Builder) CurrentBlock() (Block, bool)

// 终结指令（builder_ctrl.go；返回 llvm.Value[llvm.VoidT]）
func (b *Builder) Ret(v llvm.AnyValue) llvm.Value[llvm.VoidT]
func (b *Builder) RetVoid() llvm.Value[llvm.VoidT]
func (b *Builder) Br(blk Block) llvm.Value[llvm.VoidT]
func (b *Builder) CondBr(cond llvm.Value[llvm.IntT], then, els Block) llvm.Value[llvm.VoidT]
func (b *Builder) Switch(v llvm.Value[llvm.IntT], def Block) Switch
func (b *Builder) Unreachable() llvm.Value[llvm.VoidT]

// 算术/位运算/比较/选择（builder_arith.go）
func (b *Builder) Add/Sub/Mul(l, r llvm.Value[llvm.IntT], name string) llvm.Value[llvm.IntT]
func (b *Builder) AddNSW/AddNUW/SubNSW/SubNUW/MulNSW/MulNUW(...)   // 同上签名
func (b *Builder) SDiv/UDiv/SRem/URem/Shl/LShr/AShr/And/Or/Xor(l, r llvm.Value[llvm.IntT], name string) llvm.Value[llvm.IntT]
func (b *Builder) FAdd/FSub/FMul/FDiv/FRem(l, r llvm.Value[llvm.FloatT], name string) llvm.Value[llvm.FloatT]
func (b *Builder) Neg/Not(v llvm.Value[llvm.IntT], name string) llvm.Value[llvm.IntT]
func (b *Builder) FNeg(v llvm.Value[llvm.FloatT], name string) llvm.Value[llvm.FloatT]
func (b *Builder) ICmp(op IntPred, l, r llvm.AnyValue, name string) llvm.Value[llvm.IntT]
func (b *Builder) FCmp(op FloatPred, l, r llvm.Value[llvm.FloatT], name string) llvm.Value[llvm.IntT]
func (b *Builder) Select[T llvm.Kind](cond llvm.Value[llvm.IntT], x, y llvm.Value[T], name string) llvm.Value[T]

// 内存（builder_mem.go）
type Alloca struct{ v llvm.Value[llvm.PtrT] }   // SetAlign/Align
type Load[T llvm.Kind] struct{ v llvm.Value[T] } // SetAlign/Align
type Store struct{ v llvm.Value[llvm.VoidT] }    // SetAlign/Align
func (b *Builder) Alloca(t llvm.AnyType, name string) Alloca
func (b *Builder) Load[U llvm.Kind](p llvm.Value[llvm.PtrT], t llvm.Type[U], name string) Load[U]
func (b *Builder) Store(v llvm.AnyValue, p llvm.Value[llvm.PtrT]) Store
func (b *Builder) GEP(elem llvm.AnyType, p llvm.Value[llvm.PtrT], idx []llvm.Value[llvm.IntT], name string) llvm.Value[llvm.PtrT]
func (b *Builder) InBoundsGEP(...)  // 同 GEP
func (b *Builder) MemSet(p llvm.Value[llvm.PtrT], val, n llvm.Value[llvm.IntT], align uint32) Call[llvm.VoidT]
func (b *Builder) MemCpy(dst llvm.Value[llvm.PtrT], dstAlign uint32, src llvm.Value[llvm.PtrT], srcAlign uint32, n llvm.Value[llvm.IntT]) Call[llvm.VoidT]
func (b *Builder) MemMove(...)      // 同 MemCpy
func (b *Builder) Malloc(t llvm.AnyType, name string) Call[llvm.PtrT]
func (b *Builder) MallocArray(elem llvm.AnyType, n llvm.Value[llvm.IntT], name string) Call[llvm.PtrT]
func (b *Builder) Free(p llvm.Value[llvm.PtrT]) Call[llvm.VoidT]

// 转换（builder_cast.go；to 的种类决定结果种类）
func (b *Builder) Trunc(v llvm.Value[llvm.IntT], to IntType, name string) llvm.Value[llvm.IntT]
func (b *Builder) ZExt/SExt(v llvm.Value[llvm.IntT], to IntType, name string) llvm.Value[llvm.IntT]
func (b *Builder) FPTrunc/FPExt(v llvm.Value[llvm.FloatT], to FloatType, name string) llvm.Value[llvm.FloatT]
func (b *Builder) FPToUI/FPToSI(v llvm.Value[llvm.FloatT], to IntType, name string) llvm.Value[llvm.IntT]
func (b *Builder) UIToFP/SIToFP(v llvm.Value[llvm.IntT], to FloatType, name string) llvm.Value[llvm.FloatT]
func (b *Builder) PtrToInt(v llvm.Value[llvm.PtrT], to IntType, name string) llvm.Value[llvm.IntT]
func (b *Builder) IntToPtr(v llvm.Value[llvm.IntT], to PtrType, name string) llvm.Value[llvm.PtrT]
func (b *Builder) BitCast[U llvm.Kind](v llvm.AnyValue, to llvm.Type[U], name string) llvm.Value[U]

// 调用/聚合（builder_call.go + inst.go）
type Call[T llvm.Kind] struct{ v llvm.Value[T] } // Arg(i)/SetArg(i,v)/ArgCount
type Phi[T llvm.Kind] struct{ v llvm.Value[T] }
type Incoming[T llvm.Kind] struct{ Value llvm.Value[T]; Block Block }
func (p Phi[T]) AddIncoming(in ...Incoming[T])
func (p Phi[T]) IncomingAt(i uint32) Incoming[T]; func (p Phi[T]) Count() uint32
type Switch struct{ v llvm.Value[llvm.IntT] }   // AddCase(cond llvm.Value[llvm.IntT], blk Block) / Count()
func (b *Builder) Call[U llvm.Kind](fn llvm.Value[llvm.FnT], args []llvm.AnyValue, name string) Call[U]
func (b *Builder) PHI[T llvm.Kind](t llvm.Type[T], name string) Phi[T]
func (b *Builder) ExtractValue[U llvm.Kind](agg llvm.AnyValue, indices []uint32, name string) llvm.Value[U]
func (b *Builder) InsertValue[T llvm.Kind](agg llvm.Value[T], v llvm.AnyValue, indices []uint32, name string) llvm.Value[T]
```

### Go 类型映射（gotype.go / ir/gofunc.go）

```go
func TypeOf[T any](ctx *Context) (Type[DynT], error)
func ConstOf[T any](ctx *Context, v T) (Value[DynT], error)
func FnSignatureOf[F any](ctx *Context) (FnType, reflect.Type, error)
// ir:
func (m *Module) NewFunc[F any](name string) (Func[F], error)
```

映射表（Go → LLVM）：

| Go | LLVM | Go | LLVM |
|---|---|---|---|
| `bool` | `i1` | `float32` | `float` |
| `int`/`uint` | `Int(strconv.IntSize)` | `float64` | `double` |
| `int8..64`/`uint8..64` | `i8..i64` | `*T`/`unsafe.Pointer` | `Ptr(0)` |
| `[N]T` | `Array(T, N)` | 全字段可映射 `struct` | literal `Struct` |

不支持（→ `ErrUnsupported`）：`string`、`slice`、`map`、`chan`、`func`、`interface`、`complex`、含不可映射字段的 struct、变参函数。`ConstOf` 额外规则：指针 nil → `ConstNull`，非 nil → `ConstIntToPtr(uintptr)`；struct/array 递归。

---

## 任务分解

### 任务 1：工程基建与错误模型

**文件：** 修改 `go.mod`、`internal/binding/ErrorHandling.go`；创建 `error.go`、`error_test.go`、`doc.go`、`fatal.go`；删除旧 root 实现文件

- [x] **步骤 1：go.mod 升级**——`go 1.22` → `go 1.27`；运行 `go build ./...` 确认现状仍可编译
- [x] **步骤 2：删除旧 root 实现文件**——`block.go`/`builder.go`/`constant.go`/`context.go`/`error.go`/`execution*.go,c,h`/`global.go`/`instruction.go`/`module.go`/`pass.go`/`target.go`/`type.go`/`util.go`/`value.go`（保留 `config.go`）；`go build ./...` 确认剩余包可编译
- [x] **步骤 3：编写失败测试** `error_test.go`：`TestCatch`（`Catch` 收敛 `errPanic` 产生的 `*Error`、重抛非 `*Error`、无 panic 返回 nil）、`TestMust`（err 非 nil 时 panic）、`TestErrorMessage`（`Error()` 格式 `"op: msg"`）
- [x] **步骤 4：运行确认失败**——`go test ./...`：FAIL（`error.go` 尚不存在相关符号）
- [x] **步骤 5：实现 `error.go`**——按「核心定义」逐字实现
- [x] **步骤 6：实现 `fatal.go`**（修复旧 `error.go:20` 的逻辑反转）——先在 `internal/binding/ErrorHandling.go` 增加 Go 回调包装 `LLVMInstallFatalErrorHandlerGo(handler func(string))`（内部用 `llvmFatalErrorHandler(*C.char)` 转发，避免 root 包引入 cgo），再实现 `fatal.go`：`SetFatalErrorHandler(h func(string))`、`ResetFatalErrorHandler()`、`EnablePrettyStackTrace()`
- [x] **步骤 7：运行确认通过**——`go test ./... && go vet ./... && go build ./...`
- [x] **步骤 8：Commit**——`git add -A && git commit -m "feat: 错误模型与fatal handler修复"`

### 任务 2：生命周期与 Context

**文件：** 创建 `lifetime.go`、`context.go`、`context_test.go`

- [x] **步骤 1：编写失败测试** `context_test.go`：
```go
func TestContextCloseCascade(t *testing.T) {
	ctx := NewContext()
	closed := false
	ctx.Own(&closeRecorder{&closed}) // 辅助类型：Close 置 closed=true
	if err := ctx.Close(); err != nil { t.Fatal(err) }
	if !closed { t.Fatal("Own 的子资源未被级联关闭") }
}
func TestContextDoubleClose(t *testing.T) {
	ctx := NewContext()
	_ = ctx.Close()
	if err := ctx.Close(); err == nil || err.(*Error).Reason != ErrClosed {
		t.Fatalf("want ErrClosed, got %v", err)
	}
}
```
- [x] **步骤 2：运行确认失败**——`go test ./...`：FAIL
- [x] **步骤 3：实现**——`lifetime.go`、`context.go` 按「核心定义」实现
- [x] **步骤 4：运行确认通过**——`go test ./...`
- [x] **步骤 5：Commit**——`feat: 生命周期令牌与Context所有权`

### 任务 3：类型系统

**文件：** 创建 `kind.go`、`type.go`、`type_test.go`

- [x] **步骤 1：编写失败测试** `type_test.go`：`Int(32).String()=="i32"`、`Bits()`、`Fn` 构造与 `Params/Return/IsVarArg`、`As[FloatT]` 报 `ErrTypeMismatch`、`MustAs[IntT]`、`NamedStruct` opaque→`SetBody`→`Elems`
- [x] **步骤 2：运行确认失败**
- [x] **步骤 3：实现**——`kind.go` 12 标记；`type.go`：`Type[T]`、8 角色、构造器、`As`/`MustAs`（内部 `kindOfType(ref) Kind` 表：`LLVMIntegerTypeKind→IntT`、`LLVMFunctionTypeKind→FnT`、`LLVMPointerTypeKind→PtrT`、`LLVMStructTypeKind→StructT`、`LLVMArrayTypeKind→ArrayT`、`LLVMVectorTypeKind→VecT`、`LLVMVoidTypeKind→VoidT`、浮点族→`FloatT`、未知→`DynT`）
- [x] **步骤 4：运行确认通过**
- [x] **步骤 5：Commit**——`feat: 泛型类型系统与类型角色`

### 任务 4：值系统与桥接

**文件：** 创建 `value.go`、`value_test.go`；在 `constant.go` 落最小 `ConstInt`/`ConstBool`

- [x] **步骤 1：编写失败测试**：`v.Type().Bits()==32`、`v.MustAs[IntT]()`、`v.Dyn().As[FloatT]()` 报 `ErrTypeMismatch`、`SetName/Name`、`String()` 含 `i32 42`
- [x] **步骤 2：运行确认失败**
- [x] **步骤 3：实现**——`value.go`（`Value[T]`/`AnyValue`/`As`/`MustAs`/桥接函数/`ValueOf` 分发）与 `constant.go` 最小子集
- [x] **步骤 4：运行确认通过**
- [x] **步骤 5：Commit**——`feat: 泛型值系统与桥接API`

### 任务 5：常量全套

**文件：** 修改 `constant.go`、`constant_test.go`

- [x] **步骤 1：编写失败测试**——覆盖签名表全部构造器（含 `ConstString` 两态、负数 signed、`ConstGEP` 结果类别、跨 Context panic → `ErrCrossContext`）
- [x] **步骤 2：运行确认失败**
- [x] **步骤 3：实现**——按签名表补全 `constant.go`
- [x] **步骤 4：运行确认通过**
- [x] **步骤 5：Commit**——`feat: 常量构造全套`

### 任务 6：ir 模块/函数/块/全局

**文件：** 创建 `ir/module.go`、`ir/function.go`、`ir/block.go`、`ir/global.go`、`ir/*_test.go`

- [x] **步骤 1：编写失败测试（golden）**：`NewFunction("add", Fn(i32,[i32,i32],false))` 输出含 `define i32 @add(i32 %0, i32 %1)`；`Verify()` 成功/失败路径；`GetFunction/GetGlobal` 命中与 `(zero,false)`；`Module.Close` 后其值 `Alive()==false`；二次 Close → `ErrClosed`；`Clone` 后 `String()` 相等
- [x] **步骤 2：运行确认失败**
- [x] **步骤 3：实现**——4 文件；`NewModule` 内 `ctx.Own(m)`；`Close` 内 `life.Kill()` + `binding.LLVMDisposeModule`；`ParamAs[U]` 泛型方法做运行时 kind 校验
- [x] **步骤 4：运行确认通过**
- [x] **步骤 5：Commit**——`feat: ir模块函数块与全局值`

### 任务 7：Builder 骨架 + 预检 + 终结指令

**文件：** 创建 `ir/builder.go`、`ir/builder_ctrl.go`、`ir/inst.go`（Switch 角色雏形）、`ir/builder_ctrl_test.go`

- [x] **步骤 1：编写失败测试**——预检：未定位 → `ErrInvalidArg`；他 Context → `ErrCrossContext`；已释放值 → `ErrUseAfterFree`；golden：`ret i32 %0`、`br i1 %c, label %t, label %f`、`switch i32 %v, label %def [ i32 1, label %case ]`
- [x] **步骤 2：运行确认失败**
- [x] **步骤 3：实现**——`ir/builder.go`（`NewBuilder/Close/MoveToEnd/MoveBefore/CurrentBlock/pre/preSameType/preAlign`）、`ir/builder_ctrl.go`、`Switch` 角色
- [x] **步骤 4：运行确认通过**
- [x] **步骤 5：Commit**——`feat: builder骨架预检与终结指令`

### 任务 8：算术/位运算/比较

**文件：** 创建 `ir/builder_arith.go`、`ir/builder_arith_test.go`

- [x] **步骤 1：编写失败测试**——table-driven golden：`Add`→`add i32 %a, %b`、`AddNSW`→`add nsw …`、`SDiv`→`sdiv …`、`FAdd`→`fadd float …`、`ICmp(IntSLT,..)`→`icmp slt i32 …`、`Select`→`select i1 …`；误用：`Add` 操作数 `Int(32)` vs `Int(64)` → `ErrTypeMismatch`
- [x] **步骤 2：运行确认失败**
- [x] **步骤 3：实现**——按签名表 + 绑定映射（`LLVMBuildAdd/NSWAdd/NUWAdd/Sub/NSWSub/NUWSub/Mul/NSWMul/NUWMul/SDiv/UDiv/SRem/URem/Shl/LShr/AShr/And/Or/Xor/Neg/Not/FAdd/FSub/FMul/FDiv/FRem/FNeg/ICmp/FCmp/Select`）
- [x] **步骤 4：运行确认通过**
- [x] **步骤 5：Commit**——`feat: 算术位运算与比较指令`

### 任务 9：内存与转换

**文件：** 创建 `ir/builder_mem.go`、`ir/builder_cast.go`、`ir/*_test.go`；补全 `ir/inst.go` 的 Alloca/Load/Store

- [x] **步骤 1：编写失败测试**——golden：`alloca i32`（`SetAlign(8)`→`align 8`）、`load i32, ptr %p`、`store i32 %a, ptr %p`、`getelementptr i32, ptr %p, i64 0, i64 1`、全部 cast 各一条期望 IR、`Malloc/Free/MemSet/MemCpy/MemMove` 子串；`preAlign(3)` → `ErrInvalidArg`
- [x] **步骤 2：运行确认失败**
- [x] **步骤 3：实现**——绑定映射：`LLVMBuildAlloca/Load2/Store/GEP2/InBoundsGEP2/MemSet/MemCpy/MemMove/Malloc/ArrayMalloc/Free`、`LLVMBuildTrunc/FPTrunc/ZExt/SExt/FPExt/FPToUI/FPToSI/UIToFP/SIToFP/PtrToInt/IntToPtr/BitCast`
- [x] **步骤 4：运行确认通过**
- [x] **步骤 5：Commit**——`feat: 内存与类型转换指令`

### 任务 10：调用/PHI/聚合

**文件：** 修改 `ir/inst.go`、创建 `ir/builder_call.go`、`ir/builder_call_test.go`

- [x] **步骤 1：编写失败测试**——golden：`%r = call i32 @add(i32 %x, i32 %y)`（arg 个数不符 → `ErrTypeMismatch`）、`%p = phi i32 [ %a, %bb1 ], [ %b, %bb2 ]`、`extractvalue {i32,i64} %s, 1`、`insertvalue …`、`Call[VoidT]`
- [x] **步骤 2：运行确认失败**
- [x] **步骤 3：实现**——`Call[T]/Phi[T]/Incoming[T]` 角色 + builder 方法；绑定：`LLVMBuildCall2/PHI/AddIncoming/ExtractValue/InsertValue`
- [x] **步骤 4：运行确认通过**
- [x] **步骤 5：Commit**——`feat: 调用PHI与聚合指令`

### 任务 11：Go 类型映射

**文件：** 创建 `gotype.go`、`ir/gofunc.go`、`gotype_test.go`、`ir/gofunc_test.go`

- [x] **步骤 1：编写失败测试**：`TypeOf[int32]`→`i32`、`TypeOf[bool]`→`i1`、`TypeOf[struct{A int32; B float64}]`→`{ i32, double }`、`TypeOf[string]`→`ErrUnsupported`；`ConstOf` 标量/nil 指针/struct 递归；`NewFunc[func(int32,int32) int32]` 成功且 golden 含 `define i32 @add(i32 %0, i32 %1)`、`NewFunc[func(string)]`→`ErrUnsupported`
- [x] **步骤 2：运行确认失败**
- [x] **步骤 3：实现**——`gotype.go`（reflect switch 按映射表；`FnSignatureOf[F]`）、`ir/gofunc.go`（`NewFunc[F]`）
- [x] **步骤 4：运行确认通过**
- [x] **步骤 5：Commit**——`feat: Go类型映射与签名构造`

### 任务 12：收尾与验收

**文件：** 修改 `README.md`、`AGENTS.md`；创建 `ir/golden_test.go`、`ir/testdata/golden/main.ll`

- [x] **步骤 1：golden 端到端测试** `ir/golden_test.go`——新 API 构建 main 返回 0 的模块，完整 IR 与 `testdata/golden/main.ll` 比对
- [x] **步骤 2：README 重写**——Usage 改新 API；删除旧 API 说明；子包布局表
- [x] **步骤 3：AGENTS.md 增补**——子包边界、角色包装规则、混合错误模型、泛型方法约束
- [x] **步骤 4：全量验收**——`go build ./... && go vet ./... && go test ./...` 全绿
- [x] **步骤 5：Commit**——`docs: 新API示例与AGENTS约定`

---

## 迁移映射（旧 → 新）

| 旧 API | 去向 |
|---|---|
| `Context`/`NewContext`/`Free` | `*Context`/`Close`（任务 2） |
| `lookupType`/`lookupValue`/`lookupConstant`/`lookupInstruction` | root `type.go`/`value.go` kind 表 + `ValueOf`（任务 3-4） |
| `ConstInteger/ConstFloat/...` | `IntConst/FloatConst` + `Const*`（任务 5） |
| `Module.*` | `ir.Module`（任务 6）；`AddConstructor/AddDestructor`→P2 |
| `Function/GlobalValue` | `ir.Function`/`ir.Global`（任务 6） |
| `Builder.Create*` | `ir.Builder` 去前缀（任务 7-10） |
| `Return/Br/...` 40+ 指令别名 | `Value[VoidT]` + 角色（任务 7-10）；EH 系→P2 |
| `execution*.go/c/h`、MCJIT | 删除，P1 以 ORC 重建 |
| `target.go` DataLayout 部分 | P1-1 |
| `pass.go` | P2-6 |
| `error.go` | 任务 1 重写（含 bug 修复） |

## 自检结果

1. **规格覆盖**：§5 P0-1→任务 1-2；P0-2→任务 3-4；P0-3→任务 5；P0-4→任务 6-10；P0-5→任务 11；P0-6→任务 12。§4.1-4.5 全部落点。已补规格遗漏：`ParamAs[U]`、`Func[F].GoType()`。
2. **占位符**：无 TODO/待定；重复模式以「签名表 + 绑定映射表 + golden 用例表」给全。
3. **类型一致性**：`AnyValue` 不含 `Type()` 方法（`Value[T].Type()` 返回 `Type[T]` 不满足 `Type() Type[DynT]`），改用 `Dyn()`/`DynType()` 擦除方法；全计划统一。
